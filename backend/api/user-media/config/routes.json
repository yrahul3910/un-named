{
  "routes": [
    {
      "method": "GET",
      "path": "/media/:profile",
      "handler": "user-media.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/media/:profile",
      "handler": "user-media.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/media/:profile/:id",
      "handler": "user-media.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
